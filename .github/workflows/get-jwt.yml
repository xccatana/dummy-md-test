name: test-vault-jwt
on: workflow_dispatch

permissions:
  id-token: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Vault using JWT
        env:
          CERTIFICATE_BASE64: ${{ secrets.dev.BRIGHTDATA_ACCOUNT_ID_DEV }}
        run: |
          echo $CERTIFICATE_BASE64
